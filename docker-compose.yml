services:
  rayan_admin:
    build: .
    container_name: rayan_admin
    environment:
      - VIRTUAL_HOST=admin.medlife.tj
      - LETSENCRYPT_HOST=admin.medlife.tj
      - LETSENCRYPT_EMAIL=saburnorov@gmail.com
      - NODE_ENV=production
    expose:
      - "3000"
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
